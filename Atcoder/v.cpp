#include <iostream>
#include <bits/stdc++.h>
using namespace std;

// input m[i]
// n[i] is assending order
// if(m[i] == )
int main()
{
    int n, m;
    cin >> n >> m;
    int arr[m];
    int arrr[n];
    int temp;
    int k = 1;
    for (int i = 1; i <= m; i++)
    {
        cin >> arr[i];
        for (int j = 1; j <= n; j++)
        {
              
        }
    }

    cout << endl;

    return 0;
}
