#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, sum;
    while (cin >> a >> b)
    {
        sum = 2 * (a * b);
        cout << sum << endl;
    }

    return 0;
}
