#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int a[n][n];
    int b[n][2] for (int i = 0; i < n; i++)
    {
        for (int j = 0; i < n; j++)
        {
            cin >> a[i][j];
        }
    }
    for (int i = 0; i < n * 2; i++)
    {
        for (int j = 0; i < 2; j++)
        {
            cin >> b[i][j];
        }
    }

    return 0;
}
