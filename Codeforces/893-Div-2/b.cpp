#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main()
{

    long long n, m, d, t;
    cin >> t;
    for (int i = 0; i < t; ++i)
    {
        cin >> n >> m >> d;
        long long arr[m];
        for (int j = 0; j < n; ++j)
        {
            cin >> arr[i];
        }
    }

    return 0;
}
